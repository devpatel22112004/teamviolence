{
  "name": "teamviolence-esports",
  "version": "1.0.0",
  "description": "Team VioLencE - Professional BGMI Esports Website",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "build:all": "npm install && (cd client && npm install && npm run build) && (cd server && npm install)",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["esports", "bgmi", "tournament", "mern"],
  "author": "Team VioLencE",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
